#ifndef DEF_INCLUDE_RANDOM
#define DEF_INCLUDE_RANDOM
#include <stdio.h>
#include <stdlib.h>
#include <math.h>

// Returns a random double between min and max
double doubleRandom(double min, double max);

#endif
